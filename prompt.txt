you will be given a database schema, which contains the specific description of all the columns of all the tables in the database.please analyze which column name or value is ambiguous, may cause trouble in LLM's understanding off the schema.if the value meaning seems ambiguous, and the type of it's column is discrete instead of continuous, look up its meaning.if the column name seems ambiguous, look up its meaning.return the most possible interferences and their detailed background decument.the schema is at:"/home/walkiiiy/ChatTB/Databases/bird/bird_dev/dev_description/codebase_community.json"